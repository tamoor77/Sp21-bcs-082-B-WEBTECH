<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= vehicle ? 'Edit Vehicle' : 'Add Vehicle' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { padding: 20px; }
    .form-wrapper { max-width: 600px; margin: auto; }
  </style>
</head>
<body>
  <div class="container">
    <div class="form-wrapper">
      <h2 class="mb-4"><%= vehicle ? 'Edit Vehicle' : 'Add Vehicle' %></h2>
      <form action="<%= vehicle ? '/vehicles/admin/edit/' + vehicle._id : '/vehicles/admin' %>" method="POST" enctype="multipart/form-data">
        <div class="mb-3">
          <label for="name" class="form-label">Vehicle Name</label>
          <input type="text" name="name" class="form-control" value="<%= vehicle?.name || '' %>" required>
        </div>
        <div class="mb-3">
          <label for="brand" class="form-label">Brand</label>
          <input type="text" name="brand" class="form-control" value="<%= vehicle?.brand || '' %>" required>
        </div>
        <div class="mb-3">
          <label for="price" class="form-label">Price (€)</label>
          <input type="number" name="price" class="form-control" value="<%= vehicle?.price || '' %>" required>
        </div>
        <div class="mb-3">
          <label for="type" class="form-label">Type</label>
          <select name="type" class="form-select" required>
            <option value="">Select Type</option>
            <option value="Sedan" <%= vehicle?.type === 'Sedan' ? 'selected' : '' %>>Sedan</option>
            <option value="SUV" <%= vehicle?.type === 'SUV' ? 'selected' : '' %>>SUV</option>
            <option value="Truck" <%= vehicle?.type === 'Truck' ? 'selected' : '' %>>Truck</option>
          </select>
        </div>
        <div class="mb-3">
          <label for="image" class="form-label">Image</label>
          <input type="file" name="image" class="form-control">
          <% if (vehicle?.image) { %>
            <div class="mt-2">
              <img src="/uploads/<%= vehicle.image %>" alt="current" class="img-thumbnail" width="150">
            </div>
          <% } %>
        </div>
        <button type="submit" class="btn btn-primary"><%= vehicle ? 'Update Vehicle' : 'Add Vehicle' %></button>
        <a href="/vehicles/admin" class="btn btn-secondary">Cancel</a>
      </form>
    </div>
  </div>
</body>
</html>
